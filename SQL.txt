/*open mysql and enter your password 
then follow bellow procedure*/

step1:CREATE database hotel_db;

step2:USE database hotel_db;

/*now cheak the databases if it is created*/
step3:SHOW databases;

/*create table*/
step4:CREATE TABLE reservations(
    -> reservation_id INT AUTO_INCREMENT PRIMARY KEY,
    -> guest_name VARCHAR(255) NOT NULL,
    -> room_number INT NOT NULL,
    -> contact_number VARCHAR(10) NOT NULL,
    -> reservation_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    -> );
